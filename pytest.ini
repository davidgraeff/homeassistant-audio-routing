[pytest]
asyncio_mode = auto
# The tests import the integration as `custom_components.pipewire_audio_router`,
# so the repo root must be on sys.path. `python -m pytest` adds the CWD
# implicitly, but the bare `pytest` console script (used in CI) does not — set
# it explicitly so both invocations collect the tests.
pythonpath = .
